/// Every route name in the app, in one place. Screens expose the same value as
/// `routeName`; this class is what the router and callers refer to.
abstract final class AppRoutes {
  static const splash = '/';
  static const home = '/home';
}
